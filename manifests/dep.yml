apiVersion: apps/v1
kind: {}
metadata:
  name: deletedapp
  namespace: webserver
spec:
  replicas: 3
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      name: httpapp
      labels:
        app: webapp
    spec:
      containers:
        - image: vimal13/apache-webserver-php
          name: mycontainerofphp
